name: Breakout V1 Chronological OOS Research

on:
  workflow_dispatch:

jobs:
  backtest:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Refuse accidental holdout reuse
        run: |
          echo "2024->2025 is consumed evidence for breakout_v1_4_frozen_2026_09_08."
          echo "Do not rerun or retune against this holdout as unseen evidence."
          exit 1
