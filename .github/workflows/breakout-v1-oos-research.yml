name: Breakout V1 Chronological OOS Research

on:
  push:
    branches:
      - research/run-breakout-v1-oos
  workflow_dispatch:

jobs:
  backtest:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install research dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pandas numpy scikit-learn

      - name: Run frozen unseen 2024 to 2025 holdout once
        run: python scripts/research/run_breakout_v1_4_unseen_2025.py

      - name: Show report
        run: cat outputs/research/breakout_v1_4_unseen_2025/report.json
