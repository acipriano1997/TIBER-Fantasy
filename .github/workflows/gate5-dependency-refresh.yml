name: Gate 5 Dependency Refresh

# Temporary branch-only helper. Gate 5 removes this workflow before the final
# release PR; its only purpose is to let npm regenerate the lock graph after
# test/type tooling was reclassified as dev-only and apply compatible audit fixes.
on:
  push:
    branches:
      - codex/gate5-release-freeze
    paths:
      - 'package.json'
      - 'package-lock.json'
      - '.github/workflows/gate5-dependency-refresh.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  refresh:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: codex/gate5-release-freeze

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: '20'
          cache: npm

      - name: Reconcile dependency classification with lockfile
        run: npm install --package-lock-only --ignore-scripts

      - name: Apply only semver-compatible production audit fixes to lockfile
        run: npm audit fix --package-lock-only --omit=dev --ignore-scripts

      - name: Report residual production audit
        run: npm audit --omit=dev --audit-level=high || true

      - name: Commit refreshed lockfile
        shell: bash
        run: |
          set -euo pipefail
          if git diff --quiet -- package-lock.json package.json; then
            echo 'No dependency refresh changes were produced.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add package.json package-lock.json
          git commit -m 'Refresh Gate 5 production dependency lockfile'
          git push origin HEAD:codex/gate5-release-freeze
