node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment variables
.env
.env.local
.env.*.local

# Sleeper cache
.cache/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Phase 1 cleanup directories
.trash/
.ops/
archive/.replit
replit_move-*.txt

# Replit config — contains userenv shared secrets, must not be tracked
.replit

# local DB backups
backup_*.sql

backup_*.sql
*.dump
*.dump
*.sqlite
*.sqlite3

*.swp
*.swo

# local backups
backup_*.sql
*.swp

# TIBER Cowork plugin local config (contains API key)
# Broad, unanchored pattern so nested/duplicate copies cannot slip through.
**/plugin-config.json

# test coverage artifacts
coverage/

# temporary agent continuity file (see CLAUDE.md handoff protocol)
AGENT_HANDOFF.md

.local-dev/
